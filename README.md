# n8n-ai-agent-workflow-automation
A lightweight AI agent built using n8n that automates intelligent workflows through LLM integration. This project uses a modular workflow design to process user inputs, interact with AI models via APIs, and generate dynamic responses.  The repository includes the exported n8n workflow (JSON) with setup instructions for easy import and execution. 
